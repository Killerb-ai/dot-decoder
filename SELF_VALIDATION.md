# 무작위 이미지 자기 검증과 판독 개선

## 결론

**합성 실패 사례를 이용한 코드 개선이 가능했고, 수정에 사용하지 않은 평가 데이터에서도 개선을 확인했습니다.** 머신러닝 모델을 재학습한 것은 아닙니다. 독립적으로 만든 정답과 실패 사례를 분석해 후보 검출·격자 검증 규칙을 바꿨습니다.

생성 이미지 384장 중 개발용 192장만 보고 수정안 두 개를 비교했습니다. 두 번째 수정안을 확정한 다음 별도 평가용 192장에 기존 코드와 수정 코드를 적용했습니다. 평가 결과를 본 뒤 판독 알고리즘을 다시 조정하지 않았습니다. 양쪽 이미지의 SHA-256을 비교해 중복 이미지가 없음을 확인했습니다.

## 별도 평가 결과

| 지표 | 기존 `a1c5913` | 개선 코드 |
| --- | ---: | ---: |
| 정확한 숫자와 점 위치 | 907/1128 (80.4%) | **969/1128 (85.9%)** |
| 숫자만 일치 | 921/1128 (81.6%) | **983/1128 (87.1%)** |
| 8자리 전체 코드 일치 | 89/132 (67.4%) | **103/132 (78.0%)** |
| 아주 흐린 점의 위치 | 40/96 (41.7%) | **94/96 (97.9%)** |
| 검토 표시 없이 자동 확정한 이미지 | 63 | 57 |
| 잘못 자동 확정한 전체 이미지 | 0/192 | 0/192 |
| 오판·모호한데 검토 표시가 없던 개별 영역 | 131 | 7 |
| 점 없는 영역에 제시한 후보 수 | 159 | 159 |

점 위치가 개선된 이미지는 17장, 나빠진 이미지는 0장이었습니다. 전체 코드가 추가로 맞은 이미지는 14장이고 기존 정답을 잃은 이미지는 없었습니다. 자동 확정을 줄인 만큼 사용자 확인은 더 필요합니다. **검토 대상인 오검출 후보 159개가 사라진 것은 아닙니다.**

분모 1128은 실제 점을 넣은 영역만 집계하며, 점이 없는 영역이나 두 코드가 모두 가능한 이미지를 정답으로 부풀려 포함하지 않았습니다. 분모 132는 8자리 코드를 넣은 이미지 수입니다. 누락·빈 문서·잡음만 있는 문서·다중 가설은 전체 코드 일치율에서 제외하고 별도 오검출·확정 지표로 평가했습니다. 글자에 가려진 점은 성공 집계에서 제외하지 않았습니다.

`검토 표시 누락`은 숫자·점 좌표가 틀리거나 고유 정답을 정할 수 없는데도 해당 영역이 자동 상태인 경우입니다. 전체 복사가 차단돼도 개별 영역의 검토 누락은 별도로 집계합니다. 평가 세트에서 전체 오확정이 0건이라는 사실은 미지 문서의 오류율 0%를 뜻하지 않습니다.

## 바뀐 판독 규칙

1. 기존 임계값에서 사라지던 매우 흐린 실제 픽셀 성분을 추가 수집합니다. 격자 검증용으로만 사용하며, 고립 잡음에 독립적인 확정 점수를 주지 않습니다.
2. 모호하지 않은 공통 격자가 있고 특정 영역에 실제 성분 하나만 조금 벗어나 있으면 후보로 복구합니다. 픽셀이 없거나 여러 성분이 경쟁하면 채우지 않으며 복구 후보의 점수는 제한합니다.
3. 같은 세부분할 안에서도 물리적으로 다른 점은 경쟁 후보로 취급합니다. 점 크기·대비 차이 때문에 실제 경쟁 점을 지나치게 쉽게 무시하던 기준을 보수적으로 바꿨습니다.
4. 격자가 없거나 8개 영역 전체의 격자 근거가 부족하면 개별 후보를 검토 상태로 유지합니다.

점수만 높여서 정확한 것처럼 보이게 만들지 않았습니다. 실제로 흐린 점의 검출이 늘면서도 자동 점수는 낮고 검토 표시가 유지됩니다.

## 생성·평가 방법

- 조건 16종, 조건마다 개발 12장·평가 12장: 기본, 매우 흐림, 기울기, 배경 명암 변화, 먼지, 글자·표선, JPEG 재압축, 흐림, 크기 변경, 점별 위치 흔들림, 점 누락, 글자와 겹침, 빈 문서, 잡음만 있음, 글자만 있음, 다중 코드.
- 정답은 생성기가 먼저 선택한 숫자와 실제 그린 좌표입니다. 디코더 출력으로 정답을 만들지 않았습니다. 크기 변경 시 정답 좌표도 같은 좌표 변환으로 옮겼습니다.
- 난수 시드: 개발 `902101`, 평가 `71829011`. 생성기 버전 1이며 이미지별 시드·픽셀 크기·변형값·SHA-256을 manifest에 저장했습니다.
- 실제 앱 함수 `analyzeDocument(input, autoDetectRegion(input))`를 실행합니다. 이 합성 이미지는 크기가 작아 희소점 경로를 검증합니다. QR 해상도 일반화 학습 결과로 해석할 수 없습니다.
- 수정 전후 소스 SHA-256, 실행 환경과 조건별 집계는 [기계 판독용 결과](tests/self-validation-results.json)에 기록했습니다.

## 기존 회귀 검증

- 단위 테스트 **32개 통과**. 매우 흐린 무작위 코드 20개, 가까운 실제 성분 복구·거부, 같은 셀의 다른 점, 불충분한 격자 검토를 추가했습니다.
- 실제 문서의 앱 자동 경로: **29/31→30/31**. sample_7의 P2가 기존 정답 좌표에서 검출되며 검토 상태를 유지합니다. P7은 여전히 미검출입니다. 정답 숫자·좌표는 변경하지 않았고 회귀 하한만 5→6으로 올렸습니다.
- sample_2 변형 5종의 숫자·점 위치 **40/40 유지**.
- QR sample_9/11/8의 숫자 **24/24**, 기존 좌표 **24/24 유지**. QR 좌표는 역사적 회귀 기준입니다.
- 실제 Chrome Worker에서 업로드·보정·취소·복사 차단·모바일 화면 및 지원 밖 QR 배율 검사를 통과했습니다.

## 한계

같은 합성 생성기에서 난수만 분리한 평가이므로, 독립적으로 수집한 실제 문서 평가를 대신하지 못합니다. 페이지 테두리로 분석 영역을 정할 수 있는 조건을 사용했으며 자동 영역 추정의 모든 실패를 재현하지 않았습니다. 합성 글자는 단순한 획 패턴입니다. 실제 한글 인쇄·스캐너·원근 왜곡의 분포를 대표하지 않습니다.

겹침 조건은 점을 그린 뒤 진한 획으로 덮기 때문에 정보가 실제로 사라진 경우도 포함합니다. 이런 점을 추정해서 확정하지 않습니다. 글자만 있는 평가 이미지의 영역 1개와 다중 코드의 영역 6개는 검토 표시가 여전히 부족했습니다. 해당 이미지 전체의 복사는 차단됐습니다. 별도 평가에서 드러난 이 실패에 다시 맞춰 수정하지 않았으며 다음 개발·새 평가 세트에서 다룰 항목입니다.

## 파일과 재현 명령

- 생성기: `tests/synthetic.cjs`
- 평가기: `tests/self-validate.cjs`
- 보고서 생성: `tests/self-validation-report.cjs`
- 로컬 이미지·정답·개별 결과: `.local-tests/self-validation/`
- 브라우저로 보는 평가 이미지 목록: `.local-tests/self-validation/report.html`
- 이미지 예시와 점 확대: `.local-tests/self-validation/examples.png`

개발 의존성 설치 후 현재 코드를 새 합성 데이터에 평가할 수 있습니다.

```sh
npm install
npm test
npm run test:synthetic -- --generate --split=development
npm run test:synthetic -- --generate --split=holdout
```

두 버전 비교에는 `--source=기존-index.html`과 `--output=결과.json`을 지정합니다. 같은 manifest의 해시가 달라지면 평가를 중단합니다. 향후 수정에 이번 평가 실패 사례를 사용하면, 이번 holdout은 개발 데이터로 취급하고 새 난수·조건으로 평가 세트를 만들어야 합니다.

앱은 업로드만으로 학습하거나 코드를 자동 변경하지 않습니다. 개선은 정답을 보존한 검증·수정·새 평가 과정을 통해 축적합니다.
